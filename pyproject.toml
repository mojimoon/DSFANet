[project]
name = "ensemble-ids"
version = "0.1.0"
description = ""
authors = [
    {name = "mojimoon",email = "cryovit@outlook.com"}
]
license = {text = "MIT"}
requires-python = ">=3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
